#!/bin/sh -e

# For simplicity until I get a proper init script running, just place the lines below in /etc/rc.local.

exec 2> /var/log/rc.local.log      # send stderr from rc.local to a log file
exec 1>&2                      # send stdout to the same log file
set -x

cd /home/pi/dvc-door-sensor
#export FLASK_APP=sensor.py 
#flask run --host=0.0.0.0
python sensor.py prod

exit 0
